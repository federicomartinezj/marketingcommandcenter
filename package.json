{
  "name": "lavanti-marketing-command-center",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npx prisma generate",
    "start": "cd backend && npx prisma migrate deploy && npx tsx src/index.ts"
  },
  "engines": {
    "node": ">=22"
  }
}
