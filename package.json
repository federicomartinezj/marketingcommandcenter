{
  "name": "lavanti-marketing-command-center",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npx prisma generate && npx prisma migrate deploy",
    "start": "cd backend && npx tsx src/index.ts"
  },
  "engines": {
    "node": ">=22"
  }
}
