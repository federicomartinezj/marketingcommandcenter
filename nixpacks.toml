[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd backend && npx prisma generate && npx prisma migrate deploy"]

[start]
cmd = "cd backend && npx prisma generate && npx tsx src/index.ts"
